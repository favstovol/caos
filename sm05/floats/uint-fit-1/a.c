#include <stdio.h>
#include <stdint.h>
#include <inttypes.h>

int is_ok(uint32_t numb) {
    int least = -1;
    int most = -1;
    int cur = 1;
    while (numb != 0) {
        
        ++cur;
    }
}

int main() {
    uint32_t number;
    while ((scanf("%" PRIu32, &number)) == 1) {
        
    }
}
